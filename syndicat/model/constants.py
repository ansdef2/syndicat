"""Параметры модели. Всё, что фиксируется уставом синдиката, собрано здесь."""

# --- нормирование труда -----------------------------------------------------
HOURS_PER_MONTH = 164.3        # среднемесячная норма при 40-часовой неделе
SOCIAL_RATE = 0.30             # страховые взносы к фонду оплаты труда

# --- якорь калибровки -------------------------------------------------------
NATIONAL_WAGE_ANCHOR = 78591.4  # Росстат, янв-фев 2026, руб./мес
ANCHOR_NOTE = ("Росстат, среднемесячная начисленная заработная плата по РФ, "
               "январь-февраль 2026, предварительные данные")

# --- уставные параметры -----------------------------------------------------
PHI = 0.35        # норма социализации прибавочного продукта, φ ∈ [0;1]
PHI_RANGE = (0.25, 0.45)

GAMMA = 0.125     # шаг тарифной сетки по разрядам (ЕТКС 1-8)
DELTA = 0.12      # чувствительность к длительности производственного цикла
D0 = 30.0         # опорная длительность цикла, дней
LAMBDA = 0.30     # доля гарантии за фактическое время
QUALITY_FLOOR = 0.90   # ниже - эмиссия обнуляется
QUALITY_FULL = 0.95    # выше - множитель качества равен единице

# --- сглаживание (Хольт) ----------------------------------------------------
WAGE_DRIFT = 0.009      # демо-дрейф номинальной оплаты труда, мес (≈11,4% годовых)

ALPHA_HALFLIFE = 6.0    # полупериод сглаживания уровня, мес
BETA_HALFLIFE = 12.0    # полупериод сглаживания тренда, мес
MAX_STEP = 0.02         # предел месячного шага базы токена (якорь ожиданий)

# --- контур эмиссии ---------------------------------------------------------
RHO = 0.015             # допуск на техническую ликвидность, годовых
TOKEN_INDEX_TARGET = 1.00
TOKEN_INDEX_CORRIDOR = 0.02
PHI_SENSITIVITY = 0.5

# --- индексация заработной платы -------------------------------------------
THETA = 0.60            # коэффициент участия в приросте базы часа
ZETA = 0.50             # вес индивидуальной выработки
PERSONAL_CAP = 0.08     # ограничитель персональной надбавки, ±8 п.п.
INDEX_CAP = 0.25        # предел суммарной индексации за шаг
INDEX_STEP_MONTHS = 12  # шаг индексации, мес


def smoothing_coef(halflife_months: float) -> float:
    """alpha = 1 - 2^(-1/h): за h месяцев вес старого наблюдения падает вдвое."""
    return 1.0 - 2.0 ** (-1.0 / halflife_months)


ALPHA = smoothing_coef(ALPHA_HALFLIFE)
BETA = smoothing_coef(BETA_HALFLIFE)
